"""KeeperHub MCP client.

Scaffold placeholder. Implements the same kernel as the TypeScript client:
session bootstrap + re-init on 401/404, kh_ vs wfb_ key disambiguation,
poll-to-terminal, and single JSON-result unwrap.
"""
